<script>
    document.body.addEventListener('click', (e) => {
        const { target } = e;
        if (target.id === 'uniquePost') {
            const id = target.dataset.id;
            const apiUrl = `http://localhost:5000/post/one/${id}`;
            let req = fetch(apiUrl, {
                method: 'GET',
            })
        }
    })
</script>